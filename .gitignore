# Secrets — never commit (the API key lives here, loaded at runtime)
.secrets/
.secret/

# Personal data & generated state — never commit
state/profile.json
state/price_cache.json
state/outputs/history/
# Portfolio PDFs are personal holdings data — never commit any of them
*.pdf

# Project operating doc — kept local per preference
CLAUDE.md

# Python / tooling
.venv/
__pycache__/
.DS_Store
